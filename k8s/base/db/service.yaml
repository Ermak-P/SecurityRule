apiVersion: v1
kind: Service
metadata:
  name: mssql
  namespace: security-rule
spec:
  selector:
    app: mssql
  ports:
    - port: 1433
      targetPort: 1433
  clusterIP: None  # Headless service — app connects by DNS name "mssql"
